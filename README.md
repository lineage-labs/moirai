# @moirai savannah simulation

### Steps to run
```bash
pnpm axl:mesh:keys
pnpm axl:mesh:up
bash scripts/axl-mesh-check.sh
pnpm web 
pnpm engine
pnpm axl:mesh:down
```

`pnpm web` and `pnpm engine` - to be run on different terminals. 